# Agent Playground Environment Variables
# Copy this file to .env.local and fill in your values

# Default API Configuration (optional)
NEXT_PUBLIC_DEFAULT_ENDPOINT=https://api.openai.com/v1/chat/completions
NEXT_PUBLIC_DEFAULT_MODEL=gpt-4o-mini

# Note: All environment variables are optional
# The playground allows manual configuration through the UI
